{
  "name": "hashql-deck",
  "version": "1.0.0",
  "scripts": {
    "start": "mdx-deck deck.mdx",
    "build": "mdx-deck build deck.mdx",
    "gatsby:build": "npm run clean && gatsby build --prefix-paths && mv public build",
    "deploy": "npm run gatsby:build && npx gh-pages --dist ./build --branch gh-pages"
  },
  "devDependencies": {
    "code-surfer": "3.1.0",
    "mdx-deck": "3.0.13"
  }
}
